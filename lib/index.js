// Generated by CoffeeScript 1.6.2
(function() {
  var APuppet, Loader, comerr, cstep, outcome, sift, _;

  require("coffee-script");

  cstep = require("cstep");

  Loader = require("./loaders");

  sift = require("sift");

  outcome = require("outcome");

  comerr = require("comerr");

  _ = require("underscore");

  APuppet = (function() {
    /*
    */
    function APuppet(options) {
      this._loader = new Loader(options);
      this.load();
      this._listenOnExit();
    }

    /*
     Starts the application
    */


    APuppet.prototype.start = function(options, callback) {
      var ops;

      ops = this._parseOptions(options);
      return this.findApp(ops.name, outcome.e(callback).s(function(app) {
        return app.start({
          version: ops.version,
          args: options.args
        }, callback);
      }));
    };

    /*
    */


    APuppet.prototype.findApp = cstep(function(appName, callback) {
      var app;

      app = sift({
        name: appName
      }, this.applications).shift();
      if (app) {
        return callback(null, app);
      }
      return callback(new comerr.NotFound("application not found"));
    });

    /*
    */


    APuppet.prototype._parseOptions = function(options) {
      var nameParts;

      nameParts = options.name.split("@");
      return _.extend({}, options, {
        name: nameParts.shift(),
        version: nameParts.shift(),
        args: options.args || []
      });
    };

    /*
    */


    APuppet.prototype.load = cstep(function(callback) {
      var _this = this;

      return this._loader.loadApplications(outcome.e(callback).s(function(applications) {
        _this.applications = applications;
        return callback();
      }));
    });

    /*
    */


    APuppet.prototype._listenOnExit = function() {
      var _this = this;

      return process.on("SIGINT", function() {
        var app, _i, _len, _ref, _results;

        _ref = _this.applications;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          app = _ref[_i];
          _results.push(app.stop());
        }
        return _results;
      });
    };

    return APuppet;

  })();

  module.exports = function(options) {
    return new APuppet(options);
  };

}).call(this);
